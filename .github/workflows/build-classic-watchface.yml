name: Build WristHub Classic Watch Face
on:
  workflow_dispatch:
  push:
    branches: [ "wristhub-classic-digital-v1" ]
    paths:
      - "watchface-classic-v1/**"
      - ".github/workflows/build-classic-watchface.yml"

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.10.2"
      - name: Build APK
        working-directory: watchface-classic-v1
        run: gradle --no-daemon :watchface:assembleDebug
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: WristHub-Classic-V1
          path: watchface-classic-v1/watchface/build/outputs/apk/debug/watchface-debug.apk
          retention-days: 14
